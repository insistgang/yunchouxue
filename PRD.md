# 运筹学教学/科普官网 产品需求文档（PRD）—— v1 精品 MVP

## 1 文档信息与版本

| 项目 | 内容 |
|---|---|
| 文档名称 | 运筹学教学/科普官网 v1 产品需求文档（PRD） |
| 产品定位 | 面向大学生、自学者、运筹学爱好者的教学/科普平台 |
| 版本范围 | v1 精品 MVP |
| 文档版本 | v1.0 |
| 编写日期 | 2026-06-17 |
| 文档状态 | 已定稿（设计系统矛盾与参数不一致已修复，可据以开工；少量外部决策项见第 15.2 节决策门，绑定里程碑闭合，不阻塞 M0 准入） |
| 适用对象 | 产品、内容作者、前端工程、可视化工程、设计、测试 |
| 编写约定 | 正文以编号小节 + 表格组织；正文目标篇幅 6000–9000 中文字；公式一律 KaTeX 真渲染，文中不含任何占位符或待补内容 |

### 1.1 版本修订记录

| 版本 | 日期 | 修订人 | 修订摘要 |
|---|---|---|---|
| v0.1 | 2026-06-10 | 产品 | 初稿大纲，确定 4 个旗舰专题与技术取向 |
| v0.9 | 2026-06-14 | 产品 | 补全信息架构、设计系统、四专题规格 |
| v0.95 | 2026-06-16 | 产品 | 全文评审版，含验收标准与 DoD |
| v1.0 | 2026-06-17 | 产品 | 采纳评审意见定稿：修复设计系统三处色彩硬矛盾、统一速度区间、补 URL 分享验收、范围收敛（编辑模式后置 v2）、暗色模式决断、glossary 上线、新增成功度量/资产生产/许可/决策门/逐 island 预算等章节 |

### 1.2 术语速查（文档内高频缩写）

| 缩写 | 全称 | 含义 |
|---|---|---|
| MVP | Minimum Viable Product | 最小可行产品 |
| IA | Information Architecture | 信息架构 |
| TOC | Table of Contents | 页面目录 |
| LP | Linear Programming | 线性规划 |
| SSSP | Single-Source Shortest Path | 单源最短路径 |
| DP | Dynamic Programming | 动态规划 |
| DoD | Definition of Done | 完成定义 |
| CWV | Core Web Vitals | 核心网页指标 |
| KPI | Key Performance Indicator | 关键绩效指标 |

---

## 2 产品概述

### 2.1 背景

运筹学（Operations Research）是优化、图论、网络流、动态规划等方法的统称，是大学理工科与经管类的核心课程，也是算法工程、供应链、调度等领域的基础。但既有学习资源存在两类极端：一类是教材/讲义式的纯静态图文，符号前置、抽象难懂，劝退自学者；另一类是炫技式算法动画，热闹有余而解释不足，看完“看了热闹学不到方法”。市场缺少一个把**学术严谨**与**可交互直觉**结合、且视觉质感对标 MIT OpenCourseWare 的中文教学站。

### 2.2 产品愿景

做一本“活的运筹学入门书”：以图文教程为主轴，在关键知识点嵌入可交互的可视化演示，让读者**先建立直觉、再亲手验证、最后形式化为定义与公式**。让难懂的运筹学方法变得可看、可玩、可检验，且整站体验像一本连贯的教材而非散落的 widget 集合。

### 2.3 目标用户

- **在校大学生**：正在修运筹学/算法课程，需要把课堂上的抽象符号与几何直觉打通。
- **自学者/转专业者**：缺系统资源，需要 intuition-first 的循序渐进路径。
- **运筹学/算法爱好者**：对 explorable explanations 有审美，追求“主动阅读、读者驱动探索”的体验。

### 2.4 核心价值主张

| 价值点 | 说明 |
|---|---|
| 直觉优先 | 每个专题先用具体可操作的小例子建立几何/物理直觉，再代数化 |
| 看得见的算法 | 抽象状态量（可行域顶点、距离标号、残余网络、DP 表）实时可视化 |
| 亲手可验证 | 正文参数/系数可调，读者改动后图与结论实时联动 |
| 学术可信 | 大量留白、克制配色、模块化排版，对标精美教材，杜绝 AI 模板感 |
| 即开即用 | 纯前端静态站，零账号、零安装、秒开、可分享链接 |

---

## 3 目标与非目标

### 3.1 v1 目标（In Scope）

1. 完整首页（Hero + 4 大旗舰专题卡片 + 迷你交互“钩子” + 学习路径引导 + 关于简述）。
2. 一套可复用的设计系统（token 化的颜色/字体/字号/间距/组件）。
3. 4 个“做透”的旗舰专题，每个各配 1 个交互演示。
4. 专题列表页（可筛选/排序，为后续新增专题预留扩展性）。
5. 关于页；术语表页（内容已就绪，v1 上线）；404 页；自动生成的 sitemap。
6. 完整的交互演示通用规范（统一外壳、控件、步进、reduced-motion、无障碍、URL 状态分享）。
7. 满足性能预算、WCAG AA 无障碍、基础 SEO、移动端可用。

### 3.2 非目标（Out of Scope，v1 明确不做）

| 不做项 | 原因 / 处置 |
|---|---|
| 后端、账号、数据库 | 纯前端静态站，零服务端运行时 |
| 用户进度保存/收藏/评论 | 无账号体系；v2+ 可用 localStorage 轻量实现 |
| 全文检索引擎 | v1 用清晰 IA + sitemap；搜索取舍见 6.7 |
| 演示内的结构编辑（增删/拖拽节点·边、改权重、物品编辑器、力导向布局） | v1 收敛为“预设 + 参数滑块”，编辑能力后置 v2（见 7.x 与 13 DoD），避免范围蔓延与体积失控 |
| 用户自建图谱上云保存 | 无后端；演示状态仅经 URL query 分享（见 8.7） |
| 暗色模式独立切换开关与精修 | v1 静默跟随 `prefers-color-scheme`，保证两种模式画布可读；不提供导航切换开关（见 5.3、9.6） |
| 4 个之外的新专题内容 | IA 与模板已预留扩展，内容留待 v2+ |
| 多语言（i18n）国际化 | v1 简体中文为主，关键术语中英对照 |
| 移动端原生 App | 仅响应式 Web |

---

## 4 用户画像与典型使用场景

### 4.1 用户画像

| 画像 | 小林（大二学生） | 阿哲（转码自学者） | Mia（算法爱好者） |
|---|---|---|---|
| 背景 | 正修运筹学，期中复习 | 工科转计算机，自学算法 | 已工作，业余补理论 |
| 痛点 | 课本符号太抽象，对不上几何 | 缺系统路径，定义前置劝退 | 厌烦炫技动画，要真懂原理 |
| 诉求 | 把单纯形表与可行域顶点对上 | 循序渐进、能手算验证 | 可调参数、可单步回放 |
| 设备 | 手机为主、笔记本复习 | 笔记本 | 桌面双屏 |

### 4.2 典型使用场景

1. **课前预习**：小林从首页进入“线性规划”专题，先看可行域故事，拖动目标函数系数滑块看最优顶点滑动，再读形式化定义。
2. **手算对照**：阿哲在 Dijkstra 专题用“单步前进/后退”逐帧核对自己的手算结果，借状态解说栏确认每步松弛逻辑。
3. **深究机制**：Mia 在最大流专题切换“残余网络并排视图”，用“反向边救场网”预设理解反向边为何必要。
4. **碎片复习**：小林手机端竖屏单列阅读，TOC 收起为抽屉，演示画布等比缩放，控件移到画布下方且足够大。
5. **分享讨论**：阿哲把带特定系数的可行域演示 URL（含 query 参数，仅承载预设 + 系数 + 步进位）发给同学，对方打开即复现同一状态（验收见 8.7）。

---

## 5 信息架构与站点地图

### 5.1 IA 分层原则

采用“**主题分类 → 专题 → 章节锚点**”三层结构。v1 虽只做 4 个专题，但专题页用统一模板（导语/直觉演示/形式化/算法步骤/复杂度/延伸），让后续新增专题零摩擦套用同一设计系统。专题按运筹学知识脉络归入主题：线性规划 / 图论 / 网络流 / 动态规划。

### 5.2 站点地图

| 路径 | 页面 | 目的 | v1 状态 |
|---|---|---|---|
| `/` | 首页 Home | Hero 定位 + 4 大专题卡片网格 + 迷你交互钩子 + 学习路径 + 关于简述 + CTA | 上线 |
| `/topics` | 专题列表 | 可筛选/排序索引页，按主题/难度/标签/是否含交互过滤，承载后续新增专题 | 上线 |
| `/topics/[slug]` | 专题详情 | 单专题图文教程正文，关键知识点嵌入交互演示 island | 上线（×4） |
| `/about` | 关于 | 项目定位、面向人群、内容理念、技术与开源说明、致谢与参考、内容许可 | 上线 |
| `/glossary` | 术语表 | 运筹学核心术语中英对照与释义，支持锚点跳转，正文术语可链接至此 | 上线（内容见 16.1） |
| `/404` | 404 | 友好错误页，引导回首页/专题列表 | 上线 |
| `/sitemap-index.xml` | Sitemap | `@astrojs/sitemap` 自动生成，供搜索引擎抓取 | 上线 |

四个专题 slug：`linear-programming-simplex` / `shortest-path-dijkstra` / `max-flow-min-cut` / `dynamic-programming-knapsack`。

### 5.3 全局导航

- 品牌 wordmark（文字标 + 极简符号）→ `/`
- 专题 Topics → `/topics`（含 4 大旗舰专题下拉/megamenu，可直达详情）
- 术语表 Glossary → `/glossary`（v1 正式上线，导航常显）
- 关于 About → `/about`
- GitHub 源码外链（图标按钮，`target=_blank rel=noopener`）
- 移动端：汉堡菜单展开为全屏抽屉，焦点陷阱 + Esc 关闭，键盘可达，`aria-current` 标激活页
- **暗色模式**：v1 不提供导航切换开关，页面静默跟随系统 `prefers-color-scheme`（决策见 9.6），导航不出现暗色切换控件

---

## 6 页面与功能需求

> 每条功能给“需求描述 + 验收标准”。验收标准均可测。

### 6.1 首页（Home）

**需求描述**：建立专业可信第一印象。Hero 区一句话定位 + 学术克制视觉；精选 4 大旗舰专题卡片网格（封面/难度/时长/交互角标）；嵌入一个迷你交互演示作为“钩子”展示 explorable explanations 风格；学习路径引导（按知识脉络串联 4 专题）；关于本站简述与 CTA。

| 编号 | 验收标准 |
|---|---|
| 6.1-1 | 首页含且仅含一个 H1；Hero 主标题用 display 字级，全站唯一最大字级 |
| 6.1-2 | 专题卡片网格在 `<768px` 为 1 列、`768–1024px` 为 2 列、`≥1024px` 为 3 列 |
| 6.1-3 | 每张卡片显示分类 overline、标题、2–3 行摘要、难度、预计时长、“含交互”角标 |
| 6.1-4 | 迷你交互钩子使用 `client:visible` 延迟水合，进入视口前不加载其 JS |
| 6.1-5 | JS 未加载时，首页图文与卡片信息仍完整可读可点（渐进增强） |
| 6.1-6 | 首页总传输 JS（gzip）≤ 50 KB，CSS（gzip）≤ 20 KB |
| 6.1-7 | 卡片整卡可点（`<a>` 包裹），hover 有边框/阴影/标题色变化与轻微 translateY(-2px) |

### 6.2 专题列表（Topics Index）

**需求描述**：全部专题的可筛选/排序索引页。按所属主题、难度、标签、是否含交互过滤；卡片展示封面、摘要、阅读时长、更新日期。为后续持续新增专题提供可扩展承载页。

| 编号 | 验收标准 |
|---|---|
| 6.2-1 | 列表数据来自 content collection，构建期由 zod schema 校验，字段错误构建失败 |
| 6.2-2 | 提供至少“主题”“难度”“是否含交互”三类筛选，筛选控件有 `<label>` 关联 |
| 6.2-3 | `draft:true` 的专题不出现在列表与 sitemap |
| 6.2-4 | 筛选/排序在纯前端完成，无整页刷新；JS 关闭时默认展示全部专题 |
| 6.2-5 | 卡片显示更新日期（`updatedDate`），与详情页 `dateModified` 一致 |
| 6.2-6 | 任一筛选组合无匹配时，展示友好空状态（说明文案 + “清除筛选”CTA），不留空白页 |

### 6.3 专题详情模板（Topic Detail / TopicTemplate）

**需求描述**：单专题图文教程正文。MDX 渲染 KaTeX 公式、章节标题、配图，在关键知识点处经 Astro island 嵌入对应交互组件。含面包屑、TOC、上一篇/下一篇、相关专题、难度与时长元信息。

| 编号 | 验收标准 |
|---|---|
| 6.3-1 | 页面结构：面包屑 → H1 + 元信息行（分类·时长·更新日期）→ 导语 → 双栏（正文 prose 720px + 右侧 sticky TOC）→ 文末小结 callout + 上/下一专题分页 → 页脚 |
| 6.3-2 | 正文行宽约 68–70ch；章节间以 H2 上方 1px 衡线 + spacing-12 分隔 |
| 6.3-3 | 公式由 KaTeX 构建期预渲染为静态 HTML+MathML，运行期零公式 JS，首屏无 FOUC |
| 6.3-4 | 交互 Figure 以 `client:visible` 注水，置于相关知识点正下方 |
| 6.3-5 | TOC 仅 `≥lg` 显示，呈现 H2/H3 两级，当前阅读项用 IntersectionObserver 高亮；`<lg` 折叠为正文上方可展开“目录”抽屉 |
| 6.3-6 | 锚点跳转考虑吸顶导航偏移，目标标题不被 Navbar 遮挡 |
| 6.3-7 | 每页唯一 H1；标题层级不跳级（H1→H2→H3） |
| 6.3-8 | JS 未加载时，正文图文、公式、配图、静态替代图均可独立讲清基础内容（交互为加深而非必需） |

### 6.4 关于页（About）

**需求描述**：项目定位、面向人群、内容理念（explorable explanations）、技术与开源说明、内容许可、致谢与参考资料、联系方式。

| 编号 | 验收标准 |
|---|---|
| 6.4-1 | 包含定位、目标人群、内容理念、技术栈说明、内容许可、参考资料、联系方式七部分 |
| 6.4-2 | 注明许可（代码 MIT、内容 CC BY-SA 4.0，见 17.3）与技术栈（Astro · KaTeX）；外链 `rel=noopener` |

### 6.5 术语表（Glossary）

**需求描述**：运筹学核心术语中英对照与简短释义（如可行域 feasible region、松弛 relaxation、残余网络 residual network、最优子结构 optimal substructure）。支持锚点跳转，正文术语可链接至此。内容已就绪（见 16.1），v1 直接上线。

| 编号 | 验收标准 |
|---|---|
| 6.5-1 | 每个术语条目含中文名、英文名、简短释义，并有稳定锚点 id |
| 6.5-2 | 页面由附录 16.1 术语数据套模板渲染；正文中至少 5 个核心术语链接至本页对应锚点 |

### 6.6 导航 / 页脚 / TOC

| 编号 | 需求 / 验收标准 |
|---|---|
| 6.6-1 | Navbar 高 64px、sticky 顶吸、paper 底 + 底部 1px 分隔线，滚动后加 shadow-sm；激活项下方 2px primary 下划线，`aria-current` 标记 |
| 6.6-2 | `<1024px` 折叠为汉堡，展开为全屏抽屉，含焦点陷阱与 Esc 关闭，全键盘可达 |
| 6.6-3 | 页脚三/四列（站点简介、专题导航、关于/参考、版权许可），移动端单栏堆叠，无重色 |

### 6.7 搜索取舍

**需求描述**：v1 评估站内搜索取舍。

| 编号 | 决策 / 验收标准 |
|---|---|
| 6.7-1 | v1 不引入全文检索引擎；导航搜索图标可省略或预留入口 |
| 6.7-2 | 以清晰 IA + sitemap + 干净可读 URL 满足“找到内容”需求；如 v2 引入，优先纯前端轻量方案（如 Pagefind） |

---

## 7 四个旗舰专题详细规格

> 每个专题逐项给出：定位、学习目标、内容大纲、关键公式、交互演示规格、检查理解。所有交互演示共用第 8 节通用规范。
>
> **范围约定（v1）**：四专题交互演示在 v1 一律为“**预设 + 参数滑块 + 双向步进**”，**不含**结构编辑（增删/拖拽节点·边、改权重、物品编辑器、力导向布局），后者统一后置 v2（见 3.2 与 14.2）。各专题下文凡涉及编辑/力导向之处均标注为 v2 增强。

### 7.1 专题一：线性规划与单纯形法

**定位**：从可行域到最优顶点——看见约束如何围出一片可行域，理解单纯形法为何只需沿着顶点行走，就能找到最优解。slug：`linear-programming-simplex`，主题：线性规划，难度：进阶。

**学习目标**：
1. 把二维生产/配比问题翻译成标准形式 LP（目标 + 约束 + 非负），指出决策变量、约束系数、右端项。
2. 画出可行域、识别顶点（极点），解释“最优解必在顶点取得”及其前提。
3. 对 ≤ 型标准化最大化问题手动执行单纯形法 2–3 次迭代（选入基/出基、主元消元、读出对应顶点）。
4. 把代数基变换与几何“沿边界从一顶点走到相邻顶点”一一对应。
5. 判别并解释四种特殊情形：无可行解、无界解、多重最优解、退化，并说明几何形态。

**内容大纲**：(1) 直觉引入：桌椅工坊的产量决策；(2) 形式化定义：LP 模型的语言（标准化、松弛变量、凸性）；(3) 几何洞察：可行域、等值线与顶点最优定理；(4) 从顶点到代数：基本可行解与松弛变量；(5) 方法步骤：单纯形表迭代流程（检验数判优、最小比值检验、主元消元）；(6) 手算示例：桌椅工坊完整求解；(7) 复杂度与适用条件（Klee–Minty、内点法对比）；(8) 常见误区；(9) 延伸阅读（对偶/影子价格、敏感性分析、整数规划）。

**关键公式**（本专题“核心公式”，须有等价纯文本/aria-label，见 10.3、13 DoD#7）：

- 标准模型：$\max\ z = c^\top x \quad \text{s.t.}\ Ax \le b,\ x \ge 0$
- 引入松弛变量：$Ax + s = b,\ s \ge 0$
- 检验数（reduced cost，最大化判优）：所有非基变量 $\bar c_j \le 0$ 则当前基本可行解最优
- 最小比值检验：$\theta = \min\{\, b_i / a_{ik} : a_{ik} > 0 \,\}$
- 贯穿示例：$\max\ z = 30x_1 + 20x_2$，s.t. $2x_1 + x_2 \le 8$，$x_1 + 2x_2 \le 8$，$x_1, x_2 \ge 0$；最优 $z = 400/3 \approx 133.3$，顶点轨迹 $(0,0)\to(4,0)\to(8/3, 8/3)$。

**交互演示规格**（v1）：
- 目标：滑动目标函数系数，直观看到“等值线推动 → 最优顶点切换”的连续过程；切换“单纯形步进”模式，把每次代数换基与可行域顶点跳转严格对应。
- 可视化：半透明半平面叠加的凸可行域 + 顶点坐标标注；与梯度垂直的平行等值线族（与约束直线异色异线型）+ 梯度箭头；最优顶点用 **success 绿**强调 + 脉冲光环 + 浮层坐标/z 值；当前正在处理的入基/出基用 **accent 赭橙**；步进模式的顶点访问轨迹连线；侧栏单纯形表与图联动（高亮入基列/出基行/主元）；无界/多重最优/无可行解的文字徽标 + 几何高亮。
- 控件（v1）：目标系数滑块 $c_1, c_2$（0–50，可输入精确值）；约束右端项 $b$ 滑块（系数 $a$ 默认锁定）；模式切换（连续探索 ↔ 单纯形步进）；步进控制（上/下一步 / 自动播放 / 暂停 / 重置 / 速度 0.25x–4x）；预设下拉；图层显示开关（等值线/梯度/坐标/单纯形表/约束标签，默认全开）。
- 步进逻辑：每步四个微动作各停一拍——①判优（显示检验数，全 ≤0 标“已最优”，否则高亮最陡上升列）；②最小比值检验（仅 $a_{ik}>0$ 行显示比值，高亮最小行与主元）；③换基消元（主元行归一化+消元，变化格高亮闪动）；④几何跳转（当前顶点沿边平移到新顶点，更新坐标/z 值并加入轨迹）。每步侧栏一句自然语言解说并写入 `aria-live`。
- **完整两步示例解说**（实现须迭代至最优，勿在首个 pivot 终止）：第一次换基“x₁ 入基、s₁ 出基：从 (0,0) 走到 (4,0)，z 由 0 升到 120”；第二次换基“x₂ 入基、s₂ 出基：从 (4,0) 走到 (8/3,8/3)，z 由 120 升到 400/3≈133.3”；此后所有非基检验数 ≤0，标“已最优”。
- 预设：标准唯一最优（桌椅工坊）/ 多重最优（等值线贴边，某非基检验数恰为 0）/ 无界（找不到正系数入基列）。
- 边界：无可行解（半平面无公共交集，高亮矛盾约束，不进入步进）；无界；退化（比值并列，注明 Bland 规则避免循环）；数值显示（分数/小数标注近似，防除零）。
- 技术：SVG 绘可行域/等值线/顶点/轨迹，HTML 表格画单纯形表；几何与迭代计算用纯 TS（与渲染解耦、可单测）；CSS transition / rAF 驱动过渡；URL query 同步预设与系数（见 8.7）。
- v2 增强：自由编辑约束系数 $a$、增删约束。

**检查理解**（节选）：
1. 写出贯穿示例标准形，说明初始基与对应顶点。答：引入 $s_1,s_2\ge0$：$2x_1+x_2+s_1=8$、$x_1+2x_2+s_2=8$；初始基 $\{s_1,s_2\}$，$x_1=x_2=0$，对应顶点 $(0,0)$，$z=0$。
2. 某次迭代所有非基检验数 $\bar c_j \le 0$ 说明什么？若恰有一个 $\bar c_j=0$？答：前者说明当前已最优；后者说明存在多重最优解（等值线与某边界平行，整条边最优）。
3. 几何“走相邻顶点”对应代数什么操作？为何最小比值检验保证不越界？答：对应一次换基；$\theta$ 给出沿边能走的最大步长，取最小比值的行变量先触零出基，保证新解所有变量非负、恰落相邻顶点。

---

### 7.2 专题二：最短路径与 Dijkstra 算法

**定位**：从一个起点出发，用“贪心 + 逐步松弛”在带权图上稳稳找出到每个点的最短距离——看着距离标号与已确定集合一格格收敛。slug：`shortest-path-dijkstra`，主题：图论，难度：入门/进阶。

**学习目标**：
1. 说清最短路径问题的输入/输出/约束（非负权、有向/无向），判断场景是否适用 Dijkstra。
2. 在 6–8 节点小图手算出源点到所有点的最短距离与前驱，并回溯路径。
3. 解释 `dist[]`、`prev[]`、已确定集合 S 每步如何更新，以及松弛操作判定条件。
4. 说出朴素 O(V²) 与二叉堆 O((V+E)log V) 的来源，按稠密/稀疏选实现。
5. 识别失效条件（负权边），解释为何“一旦确定即不再改变”在负权下不成立。

**内容大纲**：(1) 直觉引入：水波、地铁与“最便宜的路”；(2) 形式化定义：带权图 SSSP、最优子结构、非负权假设；(3) 算法核心：贪心 + 松弛；(4) 算法步骤（伪代码、惰性删除工程要点）；(5) 手算示例：6 节点小图逐轮表格；(6) 复杂度与适用条件（与 BFS/Bellman-Ford/Floyd/Johnson 取舍）；(7) 常见误区；(8) 延伸（A*、双向 Dijkstra、k 短路、最短路树/DAG）。

**关键公式**（核心公式须有等价纯文本）：

- 带权图 $G=(V,E,w)$，$w: E\to\mathbb{R}_{\ge0}$；最短距离记 $\delta(s,v)$
- 松弛操作：若 $dist[u] + w(u,v) < dist[v]$，则 $dist[v] \leftarrow dist[u]+w(u,v)$，$prev[v]\leftarrow u$
- 初始化：$dist[s]=0$，其余 $dist[v]=+\infty$
- 复杂度：朴素 $O(V^2+E)=O(V^2)$；二叉堆 $O((V+E)\log V)$；斐波那契堆 $O(E+V\log V)$

**演示规模上限**（统一声明，见 7.5）：手算教学示例 6–8 节点；演示渲染推荐 ≤12 节点；v2 用户编辑硬上限 ≤16 节点。

**交互演示规格**（v1）：
- 目标：亲眼看到三个状态量随“选最小 → 加入 S → 松弛出边”循环逐步收敛；可单步追踪、可回放、可改源调速（v1 改源限于预设内切换）。
- 可视化：图本体（节点含 dist 标号、边含权重）；已确定集合 S（**success 绿**实心+勾选）；当前选中节点 u*（**accent 赭橙**脉冲圈，reduced-motion 下静态粗描边）；正在松弛的边高亮闪动并显示 $dist[u]+w$；右侧 dist/prev 实时表格同步高亮变更行；优先队列候选区（**primary 蓝**描边）标出下一个将被选出者；最短路树随推进加粗渐进生成，终点回溯路径独立强调色。
- 控件（v1）：播放/暂停；单步前进/后退（基于状态快照栈回滚）；重置/重新运行；速度滑块（0.25x–4x）；预设内选择源点 s 与目标点 t（t 出堆时提示可终止并回溯）；步进粒度切换（按事件 / 按轮）；预设下拉。
- 步进逻辑：算法离散成可回放“事件”，每事件一帧并记录完整快照（dist/prev/S/队列/当前 u/当前松弛边）。事件序列：初始化 → 选点（取 dist 最小 u*，脉冲高亮、入 S、画勾）→ 对 u* 每条出边产生松弛事件（高亮边、显示比较，成功则标号滚动+prev 重指+树边更新，失败则边变灰）→ 重复直到候选空或 t 出堆。每帧侧栏一行自然语言解说，并写入 `aria-live`。
- 预设：教科书小图（6 节点 A–F，展示“被看到 ≠ 被确定”）/ 网格地图（5×5 带权，演示水波外扩绕开高代价区，引出 A*）/ 负权陷阱（含负权边，载入弹警告，单步展示节点出堆后又现更短路却无法修正）。
- 边界：不可达节点（dist 仍 +∞，标“不可达”）；等距并列（按节点 ID 字典序，解说注明）；源=目标（dist=0 立即结束）；零权边（合法照常松弛）；负权（仅负权陷阱预设可运行，载入弹警告）；重边取较小权、自环忽略；空图/单节点优雅处理。
- 技术：SVG 渲染（便于命中测试、CSS 过渡、ARIA）；预设图用手工坐标避免力导抖动；纯函数 `dijkstraTrace(graph, s)` 一次性产出事件/快照数组，UI 只按索引渲染第 i 帧，前进/后退仅移指针；KaTeX 渲染侧栏松弛算式。
- v2 增强：自由编辑图（增删节点/边、拖拽、改权、有向/无向切换）、力导向布局。

**检查理解**（节选）：
1. 节点 dist 何时永久确定？答：被“选出”（出堆加入 S）时，而非首次被松弛时；非负权保证此刻不存在更短未发现路径。
2. 为何 Dijkstra 不能用于含负权边（即使无负环）？答：贪心正确性依赖“后续路径只增权”，负权下已确定节点可能被后续负权边缩短，但算法不回头修正，故出错；应改用 Bellman-Ford。
3. 稀疏图（E≈V）二叉堆实现复杂度与瓶颈？何时朴素 O(V²) 更优？答：$O((V+E)\log V)$，瓶颈在 V 次出堆 + E 次 decrease-key；稠密图（E≈V²）时 $O(V^2\log V)$ 劣于数组 $O(V^2)$，此时朴素更优。

---

### 7.3 专题三：最大流与最小割

**定位**：从增广路径到割的对偶——在网络里“能送多少”，等于把网络切断“最便宜要花多少”，一条定理把两个看似无关的问题焊死。slug：`max-flow-min-cut`，主题：网络流，难度：高级。

**学习目标**：
1. 用流网络三条约束（容量、反对称、守恒）形式化描述最大流问题，判断给定流量是否可行。
2. 在 ≤8 节点网络手动执行 Ford-Fulkerson / Edmonds-Karp（找增广路、算瓶颈、更新残余网络，直到无增广路）。
3. 陈述并解释最大流最小割定理，从终止残余网络读出最小割（S/T 划分与割边集）。
4. 区分“割容量”与“流过割的净流量”，说明弱对偶及等号成立条件。
5. 分析 Edmonds-Karp 复杂度 O(VE²)，指出 Ford-Fulkerson 在坏选路/无理容量下的风险。

**内容大纲**：(1) 直觉引入：从“最多能送多少货”说起；(2) 形式化定义：流网络、可行流、割；(3) 核心定理：弱对偶 → 最大流最小割定理；(4) 方法：残余网络与增广路径（反向边为何必要）；(5) 手算示例：6 节点网络推到最优并读出最小割；(6) 复杂度与适用条件（Dinic、多源多汇建模）；(7) 常见误区；(8) 延伸与应用（二分图匹配/König、图像分割、项目选择、最小费用最大流）。

**关键公式**（核心公式须有等价纯文本）：

- 可行流约束：$0 \le f(u,v) \le c(u,v)$；守恒 $\sum_{v} f(u,v) = 0\ (u \neq s,t)$
- 割容量：$c(S,T) = \sum_{u\in S, v\in T} c(u,v)$（仅算 $S\to T$ 方向）
- 弱对偶：对任意可行流 $f$ 与割 $(S,T)$，$|f| = f(S,T) \le c(S,T)$，故 $\max\text{-}flow \le \min\text{-}cut$
- 残余容量：$c_f(u,v) = c(u,v) - f(u,v)$
- 瓶颈：$\Delta = \min\{ c_f(e) : e \in P \}$
- 强对偶（最大流最小割定理）：残余网络无 $s\to t$ 增广路 $\iff$ $f$ 最大 $\iff$ 存在割使 $|f| = c(S,T)$；Edmonds-Karp 复杂度 $O(VE^2)$

**演示规模上限**（统一声明，见 7.5）：手算教学示例 6–8 节点；演示渲染推荐 ≤12 节点；v2 用户编辑硬上限 ≤16 节点。

**交互演示规格**（v1）：
- 目标：驱动 Edmonds-Karp，把“增广路径 → 瓶颈 → 残余网络更新 → 无增广路即最优 → 读出最小割”因果链可视化，建立“最大流值=最小割容量”直觉，理解反向边为何必要。
- 可视化：节点与有向边（边标“当前流量/容量”如 7/10 实时更新）；当前轮 BFS 搜索过程（**accent 赭橙**访问高亮、层次着色、增广路整条加粗）；瓶颈边特殊标记+显示 Δ；残余网络视图（正向/反向残余容量实线/虚线，可切换/并排）；终止时**最小割结果用 success 绿强调**（S/T 分区着色、割边以 success 绿高亮、“割容量=最大流值”核对面板），**不使用 danger 红**（danger 红仅用于非法/不可行，见 8.4）；顶部状态条（累计 |f|、轮数、阶段标签）。
- 控件（v1）：下一步/上一步（语义微步，可回看）；自动播放/暂停 + 速度（0.25x–4x，reduced-motion 默认暂停且步进即跳变）；预设下拉；残余网络视图开关/并排切换；“整段运行到最优”（跳过中间动画停在最小割阶段）。
- 步进逻辑：以“轮”为外层、“微步”为内层。每轮：①搜索（残余网络跑 BFS，微步出队/入队，高亮 visited 与 parent[]，显示 BFS 队列）；②找到 t 回溯增广路 P，扫描求 Δ，瓶颈边闪烁；③增广（沿 P 正向 +Δ、反向 +Δ，边标签平滑过渡，|f| 累加 Δ）；④下一轮。某轮 BFS 无法达 t → 标“已最优” → 最小割阶段（可达集为 S、其余为 T，遍历 S→T 原始边作割边以 success 绿高亮，核对面板列割边容量求和并与 |f| 比对“相等 ✓”）。每微步侧栏同步阶段/队列/visited/parent/当前增广路与 Δ/累计 |f|，并高亮“伪代码当前执行行”。
- 预设：教学经典网（6 节点，3–4 轮收敛，最小割清晰）/ 反向边救场网（贪心先走大容量路会卡死，必须靠反向边撤回）/ 对称瓶颈网（两个等值最小割，演示最小割不唯一）。
- 边界：无 s-t 路径（|f|=0，S={s}，不报错）；重边/反平行边（分别画开，残余分量独立）；零容量边（$c_f=0$ 不入队）；整数 vs 浮点（默认整数保证终止）；多条等长增广路/最小割不唯一（值唯一、割划分可能不唯一）。
- 技术：Astro island（`client:visible`）+ 轻量框架组件（Preact）管理状态机；SVG 渲染（≤12 节点，便于无障碍标注与数值过渡）；纯函数 reducer `step(state)→state'`，保存快照栈支持单步/回退；KaTeX 渲染公式与伪代码；预置坐标保证示例美观。
- v2 增强：编辑模式（拖节点、改容量、增删边、含非法校验）、力导向布局、浮点容量慢收敛提示。

**检查理解**（节选）：
1. 已知流 |f|=12 与某割容量 c(S,T)=12，能否断定 12 是最大流？答：能。弱对偶给出 max-flow ≤ min-cut；流值与割容量相等把两者夹在 12，故均为 12，该流即最大流、该割即最小割（最优性证书）。
2. 终止后如何从残余网络读最小割？为何成立？答：从 s 在残余网络做 BFS/DFS，可达集为 S、其余为 T；终止意味无 s→t 残余路径，每条 S→T 原始边满载、T→S 边流为 0，故净流量=割容量，由 |f|=净流量知 |f|=c(S,T) 且该割最小。
3. 为何要引入反向边？答：反向边（残余容量=已用流量）允许算法撤回先前次优流量、改走更优路线；无反向边则全局重排无法发生，停在次优解。

---

### 7.4 专题四：动态规划与 0-1 背包问题

**定位**：用一张表把“做不做”的指数级抉择，压缩成逐格可填的多项式递推——看清动态规划如何“用空间换重复”。slug：`dynamic-programming-knapsack`，主题：动态规划，难度：进阶。

**学习目标**：
1. 解释 DP 两大前提——最优子结构与重叠子问题，判断给定问题是否满足。
2. 写出 0-1 背包状态定义 $f(i,w)$、转移方程与边界，解释“放/不放”两分支含义。
3. 给定物品与容量 W，手填完整 DP 表，并回溯还原选中物品集合。
4. 说明 O(nW) 时间/空间复杂度，理解“伪多项式”，写出滚动数组将空间优化到 O(W)。
5. 辨析 0-1 背包与完全背包、分数背包（贪心可解）的区别，避免误用贪心或写错遍历方向。

**内容大纲**：(1) 直觉引入：装与不装的两难（贪心失败反例）；(2) DP 三关键词（重叠子问题、最优子结构、无后效性）；(3) 形式化定义：状态、转移与边界；(4) 算法步骤：从填表到回溯（含滚动数组逆序）；(5) 手算示例：4 件物品、容量 10；(6) 复杂度与适用条件（伪多项式、NP-hard、对偶 DP/meet-in-the-middle）；(7) 常见误区；(8) 延伸（完全/多重/分组背包、子集和、整数规划、Bellman 最优性原理）。

**关键公式**（核心公式须有等价纯文本）：

- 模型：$\max \sum_i v_i x_i$，s.t. $\sum_i w_i x_i \le W$，$x_i \in \{0,1\}$
- 状态：$f(i,w)=$ 只考虑前 i 件、容量 w 时的最大总价值
- 转移：$f(i,w) = \max\big(\, f(i-1,w),\ f(i-1,w-w_i)+v_i \ [\,w \ge w_i\,] \,\big)$
- 边界：$f(0,w)=0$，$f(i,0)=0$；答案在 $f(n,W)$
- 复杂度：时间 $O(nW)$，空间 $O(nW)$，滚动数组降至 $O(W)$（容量须逆序遍历）

**交互演示规格**（v1）：
- 目标：亲眼看到 DP 表逐格生长出最优解（每格值从何而来：继承上一行 vs 放入当前物品），以及如何沿表逆向回溯高亮选中物品。把转移方程与网格“正上方格”和“左上方 w−wᵢ 格”建立强烈视觉对应。
- 可视化：(n+1)×(W+1) DP 网格（行标物品含 wᵢ/vᵢ，列标容量，格显 f 值）；当前活动格用 **accent 赭橙**高亮 + 两条来源指示线（正上方“不放”、左上方“放”+vᵢ）；采纳分支角标 + 被采纳来源格闪烁；侧栏方程实例代入具体数字（如 $f(3,7)=\max(f(2,7)=7,\ f(2,3)+5=9)=9$）；回溯阶段从右下角逆向上行用 **success 绿**高亮路径，左侧物品列表点亮并累加总重/总价值；进度条（当前 (i,w)、已填格数/总格数、阶段）。
- 控件（v1）：播放/暂停；单步前进/后退（完全可逆，预记录快照）；速度滑块（0.25x–4x）；重置；预设选择器；回溯开关/跳到结果；方程联动显示开关。
- 步进逻辑：状态机两大阶段。【填表】外层 i=1..n、内层 w=0..W，每步：高亮活动格 → 画“不放”箭头 → 若 w≥wᵢ 画“放”箭头标 +vᵢ 算两候选（否则提示“放不下”）→ 侧栏渲染方程实例 → 写入 max、采纳来源闪烁 → 进度更新。【回溯】从 (n,W)：比较 $f(i,w)$ 与 $f(i-1,w)$，不等则“选中第 i 件”（物品点亮、累加、$w\mathrel{-}=w_i$），相等则“跳过”；$i$ 递减至 0。每步展示坐标、判断依据、是否选入、累计重量/价值，写入 `aria-live`。
- 预设：正文手算例（A(2,3) B(3,4) C(4,5) D(5,6)，W=10，与正文第 5 节对应）/ 贪心反例（X(10,60) Y(20,100) Z(30,120)，W=50，贪心得 160 而最优 Y+Z=220）/ 极简入门例（P(1,1) Q(2,6) R(3,10)，W=5，表小可一眼看完）。
- 边界：容量 0 或无物品（整表 0，空解不报错）；所有物品比容量重（只出“不放”分支）；多个最优解（固定规则只还原一个，注明“最优值唯一、最优方案可能不唯一”）。
- 技术：SVG（`<rect>`+`<text>`）+ 少量原生 JS / d3-selection 数据绑定，作为 `client:visible` island；来源箭头用 `<line>`/`<path>`+marker；轻量有限状态机 + 预生成步骤序列数组（每元素为一帧完整快照），单步前进/后退仅移数组索引；KaTeX 渲染侧栏方程。
- v2 增强：物品编辑器（增删物品、改重量/价值、设 W，含正整数校验）。

**检查理解**（节选）：
1. 转移为何对“放/不放”取 max？$w_i > w$ 时如何处理？答：两种互斥决策取较优；$w_i>w$ 时“放”分支不可行，$f(i,w)=f(i-1,w)$，转移前须判可行性避免越界。
2. 滚动数组为何容量须逆序？正序会怎样？答：逆序保证读取的 $dp[w-w_i]$ 是上一件处理后的旧值（对应 $f(i-1,\cdot)$），每件最多选一次；正序则可能读到本轮已更新值，退化成完全背包，结果错误。
3. 已知 $f(n,W)=13$，如何还原选了哪些物品？答：从 (n,W) 起逐行向上比较 $f(i,w)$ 与 $f(i-1,w)$，不等则第 i 件被选并 $w\mathrel{-}=w_i$，相等则未选；$i$ 递减至 0；回溯只读表不重算。

### 7.5 演示规模上限统一声明

为避免各专题节点/规模口径分散，统一如下，实现与测试以此为准：

| 维度 | 线性规划 | Dijkstra | 最大流 | 0-1 背包 |
|---|---|---|---|---|
| 手算教学示例规模 | 2 变量 / 2 约束 | 6 节点 | 6 节点 | 4 物品 / W=10 |
| v1 演示渲染推荐上限 | 2 变量 / ≤4 约束 | ≤12 节点 | ≤12 节点 | n≤8 / W≤15（表 ≤9×16 格） |
| v2 用户编辑硬上限 | ≤4 约束 | ≤16 节点 | ≤16 节点 | n≤12 / W≤30 |

---

## 8 交互演示通用设计规范

### 8.1 统一演示外壳（Reusable Demo Shell / Figure）

所有交互演示共用一套容器组件：surface/paper 底 + 1px gray-200 边 + radius-md，可 bleed 至 wide(1080)。结构固定为：

| 区域 | 内容 |
|---|---|
| 标题条 | h3 标题 + overline“交互演示 / Figure N” |
| 画布区 | Canvas/SVG/D3，最小高 320–480px，深浅底全站锁定一致 |
| 控制区 | 滑块/按钮/步进，用 secondary 与 text 按钮、滑块轨道 gray-300 + 把手 primary |
| 状态/解说栏 | 每步一行自然语言解说当前发生了什么（写入 `aria-live`） |
| caption | 可选操作说明 |

四专题各异的仅画布内核，外壳与交互语汇完全一致，降低学习成本、保证设计系统复用。

### 8.2 控件规范

- **两类核心控件（v1）**：连续参数用滑块（系数、右端项）；离散步骤用步进按钮 + 播放/暂停 + 速度调节。每个控件实时显示当前数值。结构编辑类控件后置 v2。
- **完整播放控制集**：上一步 / 下一步 / 自动播放 / 暂停 / 重置 / **速度（全站统一 0.25x–4x，支持慢放教学）**。强制**双向步进**，让读者反复琢磨某一步。
- **预设示例**：每个演示内置 2–4 个精心设计的初始配置（典型情形、退化情形、有趣反例），一键切换。
- **键盘可操作**：空格 = 播放/暂停、左右方向键 = 单步、+/- 调速；提供可见焦点环。

### 8.3 动画与步进

- 平滑缓动而非瞬跳让读者跟上状态变化，避免过长动画拖慢节奏；关键步骤自动短暂停顿；允许随时打断自动播放转入手动单步。
- 每个演示把算法离散为可回放“事件/帧”，记录完整状态快照，前进/后退仅移动指针，逻辑零重复执行。
- **状态解说栏**：每步配一行自然语言解说“为什么这么做”，是教学演示区别于炫酷动画的关键。

### 8.4 状态高亮与色彩编码（跨专题统一语义，单一真相源）

> 本表为色彩语义唯一来源，9.2 token 表与各专题可视化均以此为准；同一语义全站一种颜色，不重不串。

| 状态语义 | 语义色（token） | Hex | 辅助标识（不以颜色为唯一信息） |
|---|---|---|---|
| 已确定 / 最终最优解 / 正确结果（含最小割结果） | success 绿 | #15803D | 实心 + 勾选图标 |
| 当前处理中 / 正在选中（入基、当前节点、活动格、BFS 访问） | accent 赭橙 | #B45309 | 粗描边 + 脉冲 / “▸”标签 |
| 候选 / 可达 / 队列中 | primary 蓝 | #1D4E89 | 描边 |
| 注意 / 前提条件提示 | warning 琥珀 | #A16207 | “!” 图标 + 浅琥珀底 |
| 被排除 / 失败 / 不可行 / 约束冲突 | danger 红 | #B91C1C | “—” / “✕” + 变灰 |

**关键约定（解决评审三处色彩硬矛盾）**：
1. accent（#B45309）与 warning（#A16207）取不同色相/明度，语义互斥，不再同色。
2. “已确定 / 最优 / 最终解”全站统一为 **success 绿**；不再使用 primary-700 深蓝表示最优（9.2 同步修正）。
3. **最小割结果**属“正确结论”，用 **success 绿**强调；danger 红严格限定为不可行/失败/冲突，绝不用于标注正确答案。

### 8.5 reduced-motion 与无障碍

- **尊重 `prefers-reduced-motion`**：关闭/弱化连续动画，自动播放改为瞬时终态或单步，过渡时长趋零；以状态变化而非运动传达信息。
- **键盘可达**：所有控件原生 `<button>`、Tab 聚焦、Enter/Space/方向键操作，可见焦点环。
- **图形无障碍**：SVG 加 role/aria-label；单纯形表/DP 表/距离标号用语义化 `<table>` 或可读 DOM；每步自然语言解说写入 `aria-live="polite"` 区域播报。
- **等价文本替代**：每个演示提供与动画完全等价的状态表格/迭代日志文本视图，纯屏幕阅读器用户也能跟完整个过程。
- **触控优先**：触摸目标 ≥44×44px；滑块手柄、按钮在手机易点准；不依赖 hover（关键信息点击展开或默认常显）。
- **性能即可访问性**：`client:visible` 进入视口才水合，保障低端机/弱网可用。

### 8.6 反模式清单（设计/评审红线）

| 反模式 | 说明 |
|---|---|
| 空白沙盒陷阱 | 交互必须“轻推作者已给的例子”并主动引导注意特定现象，禁止无引导/无预设/无目标的玩具 |
| 纯动画无解释 | 必须配同步自然语言解说，问“这个动效帮助理解了什么”，答不上就删 |
| 交互即必需 | 基础理解必须在静态图文中完整，JS 不加载也能读懂 |
| 不一致交互语汇 | 禁止每专题各发明一套控件/配色/状态表示（色彩以 8.4 为唯一真相源、速度统一 0.25x–4x） |
| 认知过载 | 单屏一个核心概念 + 一个演示，一次只调一两个有意义参数 |
| 把公式当图片插入 | 始终用 KaTeX 真渲染，禁止截图 |
| 移动端坍塌 | 宽画布不溢出、手柄足够大、不依赖 hover |

### 8.7 URL 状态分享（演示状态可分享，可测）

**需求描述**：演示的关键状态（预设标识 + 参数/系数 + 当前步进位）编码进 URL query，复制链接在新标签打开即复现同一状态。仅承载内存态参数，不涉及任何服务端存储。

| 编号 | 验收标准 |
|---|---|
| 8.7-1 | 切换预设、调节系数/参数、步进时，URL query 实时反映当前状态（预设 id、参数值、step 索引），使用 `history.replaceState` 不污染历史栈 |
| 8.7-2 | 复制当前 URL 在新标签打开，演示自动加载相同预设、相同参数、定位到相同步进位；与原页视觉状态一致 |
| 8.7-3 | query 非法/越界（参数超范围、step 越界、未知预设 id）时回退到该演示默认预设并提示，不崩溃、不白屏 |
| 8.7-4 | 该功能在 4 个演示上行为一致，参数命名遵循统一约定（如 `?demo=lp&c1=30&c2=20&step=2`） |

---

## 9 视觉设计系统

### 9.1 设计哲学

学术简约：以大量留白与衡线网格承载内容，用克制的深蓝学术主色 + 中性灰阶建立专业可信感，把视觉重量让给知识与可交互演示本身。质感对标 MIT OCW / 精美教材，坚决避免 AI slop（渐变紫蓝大背景、emoji 标题、玻璃拟态卡片堆叠、千篇一律居中 hero）。

### 9.2 配色 Token（节选核心，色彩语义以 8.4 为唯一真相源）

| Token | Hex | 用途 | 白底对比度 |
|---|---|---|---|
| `--color-paper` | #FFFFFF | 页面主背景 / 正文阅读区 | — |
| `--color-canvas` | #FAFAF7 | 次级背景 / 区块分隔底（暖调） | — |
| `--color-surface` | #F8FAFC | 卡片/容器抬升底 | — |
| `--color-ink` | #1A1A2E | 标题与最高对比正文 | 17:1（AAA） |
| `--color-body` | #2D3748 | 正文主文本 | 12:1 |
| `--color-muted` | #4A5568 | 次要文本/图注 | 7.5:1（AA） |
| `--color-primary` | #1D4E89 | 学术主色：链接/主按钮/激活/品牌/“候选·可达·队列中” | 8.4:1 |
| `--color-primary-700` | #163D6B | 按钮 hover、primary 加深态（**不再用于“最优解高亮”**） | — |
| `--color-accent` | #B45309 | 强调：目标函数/增广路径/**当前处理中**（入基、当前节点、活动格、BFS 访问） | 5:1 |
| `--color-success` | #15803D | **已确定/最终最优解/正确结果（含最小割结果）/正确反馈** | 5.1:1 |
| `--color-warning` | #A16207 | 注意/前提条件提示（与 accent 区分） | 4.8:1 |
| `--color-danger` | #B91C1C | 不可行解/约束冲突/失败/被排除（**不用于标注正确结论**） | 6.3:1 |
| `--color-focus-ring` | #1D4E89 | 键盘聚焦环（3px 实心 + 2px 偏移） | — |

灰阶 50→900（#F8FAFC … #0F172A）用于填充、分隔线、边框、代码块、暗色画布。callout 四类（info/success/warning/danger）各配 `-bg` 浅底与深文字色，仅靠左侧 4px 语义粗边 + 浅底区分，不堆彩色阴影。warning 浅底用极淡琥珀，与 accent 强调区视觉可辨。

### 9.3 字体与字号

- **中文栈**：`"Source Han Sans SC", "Noto Sans SC", "PingFang SC", …`；正文一律思源黑体（v1 主字体决断见 9.7，决定全站正文用黑体）；专题大标题不另用衬线，保持单一气质。
- **拉丁栈**：`"Inter", "Helvetica Neue", Arial, …`；标题与拉丁标题同栈，不引入衬线副栈。
- **等宽栈**：`"JetBrains Mono", "Fira Code", …`，用于代码、伪代码、矩阵数据、DP 表对齐。

| 名称 | 字号 | 行高 | 用途 |
|---|---|---|---|
| display | clamp 36–48px | 1.15 | 首页 Hero 主标题（全站唯一最大字级） |
| h1 | 32px | 1.25 | 专题/文章主标题（页面唯一 H1） |
| h2 | 24px | 1.3 | 章节标题（上方衡线 + spacing-12） |
| h3 | 20px | 1.35 | 小节/卡片/Figure 标题 |
| body | 17px | 1.75 | 正文默认（中文阅读行高 1.75–1.8） |
| caption | 13px | 1.5 | 图注/元信息（muted 色） |
| code | 14px | 1.6 | 代码块/内联代码 |
| overline | 12px | 1.4 | 分类徽标（letter-spacing 0.08em） |

### 9.4 间距、圆角与阴影

- **间距**：基准 4px 的 4 点网格，梯度 4/8/12/16/20/24/32/40/48/64/80/96/128px。区块垂直间距桌面 64–96px、移动 40–48px；卡片内边距 24；callout 16–20；按钮纵 10 横 20。所有 margin/padding/gap 取自梯度，禁用奇值。
- **圆角**：sm=4px（按钮/输入/芯片）、md=8px（卡片/callout/Figure/代码块）、lg=12px（首页大卡/Hero 媒体框）、full（仅徽标胶囊）。学术风偏方正，最大不超 12px。
- **阴影**：极轻低饱和，仅用于真实抬升。xs/sm（卡片默认）、md（hover/下拉/sticky 导航）、lg（模态，少用）。优先用 1px 边框 + 留白建立层级，阴影为辅，focus 用 outline 而非 shadow。过渡统一 150ms ease / 180ms ease-out。

### 9.5 核心组件清单

Navbar、TopicCard、Button（Primary/Secondary/Text 三级，统一高 40px）、CodeBlock、MathBlock、Callout（info/success/warning/danger）、TopicTemplate、Figure（交互演示外壳）、Footer、Breadcrumb、TOC。所有组件以 `.astro` scoped style 实现，token 集中于 `tokens.css` 作为单一真相源。

### 9.6 暗色模式决断（v1）

- **决策**：v1 **不提供暗色切换开关**，页面静默跟随系统 `prefers-color-scheme`。
- token 体系已为变量切换预留，但 v1 仅保证两种系统偏好下页面与 4 个演示画布**均可读、对比度不破线**（正文文本 ≥4.5:1，UI/图形 ≥3:1）；暗色精修留待 v2。
- 导航不出现暗色切换控件（与 5.3 一致）；DoD 第 13 节据此验收（暗色仅验“可读不破线”，不验完整精修）。

### 9.7 主字体决断（v1）

- **决策**：全站正文与标题统一使用**黑体（思源黑体 / Noto Sans SC）**，不引入衬线副栈，保证单一气质与可控的字体加载；衬线教材质感留待 v2 评估。
- 中文字体子集化 + woff2 + `font-display:swap` 自托管，按页统计字符集控制体积（见 15.1 风险缓解）。

---

## 10 技术架构

### 10.1 框架与项目结构

Astro 5.x（`output:'static'`），内容驱动 + 组件化 + islands 架构：教程正文以零 JS 静态 HTML 交付，仅交互演示作为 island 按需水合。核心目录：

```
src/
├── content.config.ts        # 'topics' collection + zod schema
├── content/topics/*.mdx      # 4 个旗舰专题（新增专题只需加文件）
├── data/glossary.ts          # 术语表数据（渲染 /glossary）
├── components/
│   ├── ui/                  # Card, Badge, Breadcrumb, TOC, Callout, Prose
│   ├── layout/              # Header, Footer, Nav, MobileMenu
│   ├── seo/                 # BaseHead, JsonLd
│   └── visualizations/      # 4 个 island + _shared/（PlayControls, GraphCanvas, Slider, colorTokens, reducedMotion, urlState）
├── layouts/                 # BaseLayout, TopicLayout
├── pages/                   # index, about, glossary, 404, topics/{index,[slug]}
├── styles/                  # tokens.css, global.css, katex-overrides.css
└── lib/                     # simplex.ts dijkstra.ts maxflow.ts knapsack.ts geometry.ts（纯逻辑，可单测）
```

### 10.2 技术选型

| 技术 | 用途 |
|---|---|
| Astro 5.x + Content Collections (zod) | 类型安全内容层，frontmatter 构建期校验，新增专题可无限扩展 |
| MDX | Markdown 中 import 嵌入可视化组件 |
| remark-math + rehype-katex + KaTeX | 构建期渲染公式为静态 HTML+CSS，运行期零公式 JS |
| 原生 Canvas/SVG + 按需 D3 子模块 | 图与动画；仅引 d3-scale/d3-shape/d3-selection 等子包，杜绝整包 d3 |
| TypeScript (strict) | 内容 schema、算法求解器、组件 props 全类型化；lib 纯函数可单测 |
| 原生 CSS + CSS 变量 (tokens.css) | 不引 Tailwind/UI 库，避免 class 噪声与廉价模板感 |
| 交互框架 | 优先纯 TS/原生；复杂状态 island 统一用 Preact(~4KB)（统一选型见 15.2 决策门） |
| astro:assets (Sharp) | 封面/插图 AVIF/WebP + 响应式 srcset + 懒加载 |
| @astrojs/sitemap | 构建期自动生成 sitemap |

### 10.3 公式渲染

采用 KaTeX 构建期预渲染（首选）：构建阶段用 `renderToString`（remark-math + rehype-katex）编译为静态 HTML，运行期零 JS、首屏即出公式、无 FOUC。输出 `htmlAndMathml`（同时产 MathML 供辅助技术）；KaTeX 字体自托管 + preload 关键字体 + font-display，避免 CDN 抖动与字形回退；构建期开 `throwOnError` 及早发现 LaTeX 笔误；`displayMode` 区分行间/行内；公式用 `currentColor`/CSS 变量适配主题。

**无障碍补强（量化“关键公式”）**：KaTeX 隐藏 MathML 在部分屏幕阅读器朗读不佳，故对**核心公式**补人工 `aria-label` 或邻近纯文本解释。**“核心公式”客观界定**：每专题“学习目标”所列的转移方程/判优条件/对偶定理等核心式，即 7.1–7.4 各专题“关键公式”小节中标注为核心的条目。**量化要求**：每个专题至少 4 个核心公式具备等价纯文本描述（线性规划：标准模型、检验数判优、最小比值、贯穿示例；Dijkstra：松弛、初始化、复杂度、最优子结构；最大流：可行流约束、割容量、弱对偶、最大流最小割定理；背包：模型、状态、转移、复杂度）。DoD #7 据此验收。

### 10.4 构建与部署

纯静态导出（`astro build` → dist/），公式与图文构建期完成渲染，无服务端运行时。pnpm + lockfile。质量门禁：`astro check`（TS + 内容 schema）与 `vitest`（lib 算法单测）作为构建前置。

**部署平台决断（v1）**：选 **Vercel**（零配置、PR 预览、全球 CDN、自动 HTTPS、OG 动态图能力更强，无需 base 路径特殊处理）；GitHub Pages 作为备选（开源零成本但需配置 base 路径）。CI（`.github/workflows/ci.yml`）：push 到 main → checkout → setup pnpm/node(缓存) → `pnpm install --frozen-lockfile` → `astro check` → `vitest run` → `astro build` → 部署；PR 仅跑 check+test+build 预检并出 Vercel 预览；Lighthouse CI 作性能/可访问性回归门禁。构建产物 hash 命名 + 长期缓存(immutable)。

### 10.5 性能预算

| 指标 | 目标 |
|---|---|
| 首屏阻塞 JS | 0 或近 0 KB（正文/公式静态，交互全走 island 延迟水合） |
| 首页总传输 JS (gzip) | ≤ 50 KB；CSS (gzip) ≤ 20 KB |
| 水合策略 | 默认 `client:visible`，首屏外 `client:idle`，绝不全量 `client:load` |
| LCP | < 2.0s（4G/中端机） |
| INP | < 200ms |
| CLS | < 0.1（图像显式宽高 + 自托管字体 font-display:swap） |
| Lighthouse 性能 | ≥ 95 |

**逐 island JS 预算拆解（gzip，统一上限 50KB/island，目标 ≤30KB）**：

| island | 目标 | 硬上限 | 关键控制手段 |
|---|---|---|---|
| 线性规划（SVG + 纯 TS 求解，无框架） | ≤18 KB | 30 KB | 原生 SVG，几何/单纯形纯 TS；不引框架 |
| Dijkstra（SVG + Preact） | ≤24 KB | 40 KB | Preact ~4KB + 预生成事件数组；预设手工坐标，无力导向 |
| 最大流（SVG + Preact + 残余视图） | ≤28 KB | 45 KB | Preact + reducer 状态机；残余并排视图共用渲染；无力导向、无编辑 |
| 背包（SVG + d3-selection） | ≤16 KB | 28 KB | 仅 d3-selection 子包；预生成帧数组 |

**降级路径**：v1 因移除编辑模式与力导向（d3-force），最大流 island 体积风险大幅下降；若任一 island gzip 实测逼近硬上限，按序降级：①去掉非核心过渡动画 → ②残余视图改按需 `import()` 懒加载 → ③复杂态拆为更小帧。**CI 门禁**：构建后对 4 个 island 产物逐一 gzip 实测，超硬上限即 CI 红灯（见 10.4、13 DoD#5）。

### 10.6 SEO

每页唯一 `<title>` 与 meta description（由 `topic.summary` 注入，BaseHead 统一管理）；canonical 链接；Open Graph + Twitter Card（image 用专题封面 1200×630）；JSON-LD 结构化数据（详情页 Article/LearningResource，含 datePublished/dateModified/author/educationalLevel=difficulty；列表页 ItemList/BreadcrumbList；站点级 WebSite）；`@astrojs/sitemap` + robots.txt 声明 sitemap；语义化 HTML 与单一 H1；`lang='zh-CN'`；关键术语中英对照提升长尾；图片 alt（coverAlt）；干净可读 URL，分享 query 不影响 canonical。

### 10.7 可访问性（技术层）

语义化结构（header/nav/main/article/footer + 正确标题层级）；Skip-to-content 链接置于 body 首；全键盘可操作（导航、移动抽屉焦点陷阱+Esc、演示控件原生 button）；可见 focus-visible 轮廓；交互可视化提供等价文本/数据表与 `aria-live` 步骤播报；色彩对比 ≥ WCAG AA（正文 4.5:1，大字/UI 3:1）且不以颜色为唯一编码；`prefers-reduced-motion` 降级；装饰元素 `aria-hidden`；筛选控件 `<label>` 关联；目标命中 ≥44×44px；链接文本可辨识（避免“点击这里”）。

---

## 11 内容创作规范

### 11.1 写作风格

- **三段式节奏**：先建立疑问/直觉 → 给出演示让读者亲手验证 → 再形式化为定义与公式。避免一上来甩定义和符号。
- **intuition-first**：每个专题先用具体可操作小例子建立几何/物理直觉，再引入代数表述与算法步骤，最后给复杂度/正确性结论。
- **克制信息密度**：单屏只讲一个核心概念配一个演示，用留白、衡线、模块化排版承载专业可信。
- **叙事连贯**：4 个专题用一致视觉语言与交互语汇，体现运筹学知识脉络（优化/图论/网络流/动态规划），让读者感到这是“一本书”而非散落 widget。
- **每专题统一模板章节**：导语 / 直觉引入 / 形式化定义 / 算法步骤 / 手算示例 / 复杂度与适用条件 / 常见误区 / 检查理解 / 延伸阅读。

### 11.2 公式规范

始终用 KaTeX 真渲染（禁止截图）；块级公式 `$$...$$` 居中、可横向滚动、可选右侧编号；行内公式 `$...$` 随文流对齐基线。关键推导步骤间留白，可配左侧 1px primary 强调线作推导块。核心公式按 10.3 补无障碍文字描述。

### 11.3 代码与伪代码规范

用 CodeBlock 组件：gray-100 底 + radius-md + 1px 边，等宽 14/1.6；语法高亮低饱和（关键字 primary、字符串 success、注释 faint），避免霓虹色；可选标题条（语言标签 + 复制按钮）与行号；横向溢出可滚动，tab=2 空格。伪代码与演示侧栏“当前执行行”高亮一一对应。

### 11.4 术语表与中英对照

关键术语首次出现给中英对照（如可行域 feasible region、松弛 relaxation、残余网络 residual network、最优子结构 optimal substructure、检验数 reduced cost）。术语在正文可链接至 `/glossary` 对应锚点。frontmatter 内容模型字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| title / slug / summary | string (required) | 标题/URL 短标识/≤160 字摘要 |
| difficulty | enum 入门/进阶/高级 | 难度角标 |
| readingTime | number/auto | 预计时长（remark 可自动估算） |
| tags / theme | string[] / enum | 标签 / 主题分类 |
| cover / coverAlt | image / string | 封面（astro:assets）/ 替代文本 |
| hasInteractive / interactiveComponent | boolean / string | 是否含交互 / 组件名 |
| publishDate / updatedDate | date | 发布/更新日期 |
| prerequisites / references | string[] / {label,url}[] | 前置专题 / 参考链接 |
| draft / order | boolean / number | 草稿开关 / 精选排序 |

---

## 12 非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | 满足 10.5 性能预算（含逐 island 预算）；CWV：LCP<2.0s、INP<200ms、CLS<0.1；Lighthouse 性能≥95 |
| 可访问性 | 满足 WCAG 2.1 AA：对比度、键盘可达、焦点可见、reduced-motion、aria-live、等价文本替代；目标命中≥44px |
| SEO | 唯一 title/description、canonical、OG/Twitter Card、JSON-LD、sitemap、语义化 HTML、`lang=zh-CN` |
| 浏览器兼容 | 最新两个主版本 Chrome/Edge/Firefox/Safari（桌面 + iOS/Android）；不支持 IE；含 iOS Safari 专项测试（见 12.1） |
| 响应式 | 移动优先，断点 sm480/md768/lg1024/xl1280；卡片 1/2/3 列；详情页 `<1024` 单列+TOC 抽屉、`≥1024` 双栏；clamp() 流式缩放；Canvas 按 devicePixelRatio 缩放保清晰 |
| 渐进增强 | JS 未加载时图文/公式/卡片/导航/静态替代图完整可用；交互为加深而非必需 |
| 正确性 | 算法求解器为教学站底线，lib 纯函数有单元测试覆盖核心/退化/边界路径，与手算示例交叉验证 |

### 12.1 移动端与 iOS Safari 专项兼容测试条目

| 编号 | 测试条目 |
|---|---|
| 12.1-1 | iOS Safari 上 SVG/Canvas 按 devicePixelRatio 渲染清晰无糊；横竖屏切换不变形 |
| 12.1-2 | 触控单击/拖动滑块在 iOS Safari 不触发误缩放（`touch-action` 配置正确） |
| 12.1-3 | 375px 宽下 4 个演示画布等比缩放不溢出、控件下移、手柄 ≥44px |
| 12.1-4 | 演示在不支持/JS 加载失败时显示静态替代图 + 文字说明（兜底态，见 13 DoD#3） |
| 12.1-5 | Android Chrome 与 iOS Safari 各跑一遍 4 演示双向步进，无卡死、无白屏 |

---

## 13 验收标准 / v1 Definition of Done

v1 视为完成（DoD）需同时满足：

1. **页面齐全**：首页、`/topics`、4 个 `/topics/[slug]`、`/about`、`/glossary`、`/404`、sitemap 全部上线。
2. **4 个旗舰专题内容完整**：每个含第 11.1 节九个标准章节、关键公式 KaTeX 渲染、≥3 道检查理解，且数据与 DP 表/单纯形表/网络求解严格自洽。
3. **4 个交互演示全部可用**：各配 ≥2 个预设、完整双向步进 + 播放/暂停/速度（统一 0.25x–4x）/重置、状态解说栏、统一外壳与 8.4 色彩语义；4 个专题边界情形均正确处理不报错；**不含**结构编辑（v2）；**演示在 JS 失败/不支持时有静态替代图兜底**。
4. **设计系统落地**：tokens.css 为单一真相源；色彩语义以 8.4 为唯一来源（accent≠warning、最优=success 绿、最小割=success 绿、danger 仅失败）；10 个核心组件可复用；无 AI slop 反模式。
5. **性能达标**：4 个 island JS 各 ≤对应硬上限且 CI 逐一 gzip 实测通过；首页 JS ≤50KB / CSS ≤20KB gzip、Lighthouse 性能 ≥95、CWV 三项达标。
6. **无障碍达标**：键盘可全程操作 4 个演示与导航；演示有等价文本视图与 aria-live 播报；对比度 AA；reduced-motion 生效。
7. **公式渲染**：全站公式构建期 KaTeX 预渲染、运行期零公式 JS、无 FOUC；**每专题 ≥4 个核心公式具备等价纯文本/aria-label**（见 10.3）。
8. **响应式与兼容**：375px / 768px / 1280px 三档下页面与演示均可读可操作，无溢出、无手柄过小、无 hover 依赖关键信息；通过 12.1 iOS Safari/Android 专项条目。
9. **URL 状态分享**：4 个演示满足 8.7 全部验收（query 同步、复制复现、非法回退、命名统一）。
10. **暗色可读**：两种系统 `prefers-color-scheme` 下页面与 4 演示画布均可读、对比度不破线（不验完整暗色精修）。
11. **构建门禁通过**：`astro check` 与 lib 单测（vitest）通过，CI 绿灯并成功部署到 Vercel。
12. **SEO**：每页唯一 title/description/canonical、OG 图、详情页 JSON-LD、sitemap 收录。

---

## 14 里程碑、排期与迭代路线

### 14.1 v1 里程碑（含工期与人力估算）

> 估算以 1 名前端/可视化工程 + 0.5 设计 + 0.5 内容（折合人天）为基线，按 5 个交付里程碑顺序推进，总计约 38–46 人天。

| 阶段 | 里程碑 | 主要交付 | 估算 | 负责人 |
|---|---|---|---|---|
| M0 | 地基 | 项目脚手架、tokens.css、BaseLayout、content schema、Vercel 部署 + CI 打通、字体子集化 | 5–6 人天 | 前端 |
| M1 | 设计系统 + 首页 | 10 个核心组件、首页（含 1 个迷你交互钩子）、glossary 页 | 7–8 人天 | 前端 + 设计 |
| M2 | 专题模板 + 列表 | TopicTemplate、TOC、面包屑、`/topics` 筛选（含空状态）、关于页 | 6–7 人天 | 前端 |
| M3 | 演示 ×4 | `_shared` 控件 + urlState + 4 个 island（含 lib 算法 + vitest 单测） | 12–14 人天 | 可视化 |
| M4 | 内容定稿 | 4 专题 MDX 全文、公式、检查理解、术语对照、封面与静态替代图 | 5–6 人天 | 内容 + 设计 |
| M5 | 打磨 + 验收 | 无障碍/性能/响应式/iOS 回归、逐 island 体积门禁、对照 DoD 验收，上线 | 3–5 人天 | 全员 |

### 14.2 v2+ 展望

- 演示**结构编辑能力**（图/网络/物品编辑器 + d3-force 力导向布局），独立做体积预算与降级。
- 持续新增专题（对偶理论与影子价格、整数规划分支定界、Bellman-Ford、A*、二分图匹配、最小费用最大流等），套用同一模板与设计系统零摩擦扩展。
- 前置专题图谱与学习路径可视化；术语正文自动链接增强。
- 站内轻量搜索（Pagefind 类纯前端方案）。
- 暗色模式完整设计精修与切换开关；衬线教材字体气质评估。
- localStorage 进度/书签（仍无后端）；可选 MathJax v4 替代渲染以增强公式无障碍。
- 可探索性增强：正文行内可拖动数字（Tangle 式 reactive document）。

---

## 15 成功度量、风险与决策门

### 15.1 风险登记

| 风险 | 影响 | 缓解 |
|---|---|---|
| 中文字体子集化不当导致 CLS/体积膨胀 | 性能预算超标 | 自托管可变字体 woff2 + 中文子集化 + font-display:swap，按页统计字符集 |
| 演示 island JS 超硬上限 | 性能门禁失败 | v1 移除编辑/力导向降风险；逐 island 预算 + CI gzip 门禁 + 三级降级路径（10.5） |
| KaTeX 公式无障碍朗读不佳 | AA 风险 | 每专题 ≥4 核心公式补 aria-label/纯文本（量化于 10.3）；v2 评估 MathJax v4 |
| 双向步进的状态快照内存/实现复杂度 | 工期/性能 | 预生成步骤序列数组，单步仅移指针；按 7.5 限制演示规模上限 |
| 算法求解器正确性缺陷 | 教学可信度受损（底线） | lib 纯函数单测覆盖典型/退化/边界用例，与手算示例交叉验证 |
| 视觉滑向 AI slop | 品牌定位失败 | 评审对照 8.6 反模式红线，以排版功力而非视觉特效达成质感 |
| 移动端宽画布溢出/触控不准 | 移动可用性 | 容器查询/ResizeObserver 自适应，控件下移加大，触摸目标≥44px，iOS 专项测试 12.1 |

### 15.2 决策门（外部待决项，绑定里程碑闭合，不阻塞 M0 准入）

> 评审指出的设计系统矛盾、参数不一致、范围蔓延、暗色三方打架、glossary/字体/部署平台等已在本稿内**就地决断**（见各对应小节）。以下为仍需相关方书面确认的少量项，已绑定最迟决策里程碑与负责人；其默认决策已写入正文，确认前按默认值推进，不阻塞开工。

| 待决项 | 默认决策（正文已采纳） | 最迟决策门 | 负责人 |
|---|---|---|---|
| 部署平台 | Vercel（10.4） | M0 前确认 | 工程负责人 |
| 主字体 | 全站黑体、无衬线副栈（9.7） | M0 前确认 | 设计负责人 |
| 复杂 island 框架 | 统一 Preact（10.2） | M3 前确认 | 可视化负责人 |
| 首页迷你交互钩子 | 复用线性规划演示的轻量裁剪版（仅系数滑块 + 等值线，无步进） | M1 前确认 | 产品 + 可视化 |
| 内容许可 | 代码 MIT + 内容 CC BY-SA 4.0（17.3） | M4 前确认 | 产品负责人 |

### 15.3 成功度量与分析

为衡量“精品 MVP”是否达成教学目标，定义如下指标与采集方式：

| 类型 | 指标 | 目标/口径 |
|---|---|---|
| 北极星 | 专题完读率（滚动到文末小结的会话占比） | ≥ 35% |
| 辅助 | 演示交互率（进入演示视口后发生 ≥1 次步进/调参的会话占比） | ≥ 50% |
| 辅助 | 演示深度（每次交互会话平均步进次数） | ≥ 5 步 |
| 辅助 | 跳出率 / 平均停留时长 | 跳出 < 55%，停留 > 2 分钟 |

**采集方式**：接入隐私友好、无 Cookie、不采集个人数据的轻量分析（**Plausible 或 Umami**，与“无后端/不要账号”取向一致，仅前端打点聚合）；事件埋点限于“演示步进/调参/预设切换/完读锚点到达”等匿名交互事件。若上线前隐私评估未通过，则 v1 不接入分析、仅保留 CWV 实测，指标延至 v2 衡量。

---

## 16 内容与素材生产计划

### 16.1 术语表（运筹学核心术语中英对照，渲染 /glossary）

| 中文 | 英文 | 简释 |
|---|---|---|
| 可行域 | feasible region | 满足全部约束的解的集合，线性约束下为凸多面体 |
| 极点 / 顶点 | extreme point / vertex | 可行域不可被内部线段表示的角点，对应基本可行解 |
| 松弛变量 | slack variable | 把 ≤ 不等式化为等式引入的非负变量，其值=资源剩余量 |
| 检验数 | reduced cost | 单纯形法判优依据，最大化下全 ≤0 即最优 |
| 影子价格 | shadow price | 约束右端项每增一单位带来的目标增量，资源边际价值 |
| 松弛（操作） | relaxation | 最短路中用 `dist[u]+w<dist[v]` 更新距离上界 |
| 距离标号 | distance label | Dijkstra 中 `dist[v]`，已知源到 v 的最短距离上界 |
| 残余网络 | residual network | 含反向边、残余容量的网络，表示可继续推送/撤回的流 |
| 增广路径 | augmenting path | 残余网络上 s→t 的路径，可沿其增加流量 |
| 最小割 | minimum cut | 容量之和最小的 s-t 割，等于最大流值 |
| 最优子结构 | optimal substructure | 整体最优解由子问题最优解拼装而成 |
| 重叠子问题 | overlapping subproblems | 朴素递归反复求解同一子问题，DP 用记忆化避免 |
| 伪多项式 | pseudo-polynomial | 复杂度依数值大小（如 W）而非输入位数，0-1 背包本质 NP-hard |

### 16.2 视觉素材生产计划

| 资产 | 规格 | 数量 | 负责人 | 里程碑 |
|---|---|---|---|---|
| 专题封面图 | 1200×630（OG 同用），AVIF/WebP 经 astro:assets，学术克制风格 | 4 张 | 设计 | M4 |
| 演示静态替代图 | 演示终态/关键步快照（JS 失败兜底，SVG 或 PNG，含 alt） | 4 张 | 可视化 + 设计 | M4 |
| 章节配图/示意图 | 内联说明图（可行域、图、网络、DP 表骨架），矢量优先 | 按需 6–10 张 | 设计 | M4 |
| Hero / 品牌符号 | wordmark + 极简符号 SVG | 1 套 | 设计 | M1 |

所有封面与插图须遵守 9.1 反 AI slop 与 8.6 反模式；演示静态替代图须与对应 island 终态一致，供渐进增强兜底（DoD#3、12.1-4）。

---

## 17 附录

### 17.1 参考资料

- Bret Victor — *Explorable Explanations*（reactive documents / 引导式交互的设计哲学）
- Setosa / Explained Visually — intuition-first 节奏与极简学术排版标杆
- Tangle.js — 正文行内可调参数的 reactive document 实现参照（v2 增强）
- VisuAlgo — 算法可视化播放控制与键盘方案、状态解说栏蓝本
- USFCA Data Structure Visualizations — “克制即专业”的极简动画基准
- MIT OpenCourseWare — 学术简约视觉与可信度质感对标
- Astro Islands 架构文档 — `client:visible`/`client:idle` 渐进增强落地依据
- KaTeX 文档与 a11y 讨论 — 构建期预渲染方案与无障碍边界
- MathJax v4 Accessibility — 公式无障碍更强的替代/补充方案（v2 评估）
- 教材：Bertsimas《Introduction to Linear Optimization》、Vanderbei《Linear Programming》、CLRS《算法导论》（第 24/26 章）、Kleinberg-Tardos（第 7 章）、Sedgewick《Algorithms》

### 17.2 评审采纳与修订对照

| 评审条目 | 处置 |
|---|---|
| 文首 LLM 自述泄漏 | 已整段删除，文档直接以一级标题开篇 |
| accent 与 warning 同色 | warning 改琥珀 #A16207，与 accent #B45309 区分（8.4/9.2） |
| 最优解高亮蓝/绿冲突 | 全局统一 success 绿；删 9.2“最优=primary-700”表述（8.4/9.2） |
| danger 红同标失败与最小割 | 最小割改 success 绿；danger 仅失败/不可行（8.4/9.2/7.3） |
| 速度区间不一致 | 全站统一 0.25x–4x（8.2 及各专题） |
| URL 分享无验收 | 新增 8.7 四条可测验收 + DoD#9 |
| 编辑模式范围蔓延 | v1 收敛为预设+滑块，编辑/力导向后置 v2（3.2/7.x/14.2） |
| 暗色模式三方打架 | 决断 v1 不提供开关、仅跟随系统并保证可读（5.3/9.6/3.2/DoD#10），关闭悬置 |
| glossary 状态混乱 | v1 直接上线（5.2/6.5/DoD#1） |
| 文档名实不符 | 闭合矛盾后定为 v1.0 已定稿，状态对齐（第 1 节） |
| 未决问题影响排期 | 已就地决断，余项绑定决策门 + 负责人 + 里程碑（15.2） |
| 最大流节点口径分散 | 7.5 统一演示规模上限表 |
| 单纯形仅一步示例 | 7.1 补完整两步迭代至 (8/3,8/3) |
| 关键公式无判定标准 | 10.3 客观界定 + 每专题 ≥4 量化（DoD#7） |
| 筛选零结果态缺失 | 6.2-6 空状态验收 |
| 缺成功度量 | 新增 15.3 成功度量与隐私友好分析 |
| 缺逐 island 预算 | 10.5 四 island 预算 + 降级 + CI 门禁 |
| 缺素材生产计划 | 16.2 视觉素材生产计划 |
| 缺内容许可 | 17.3 明确 MIT + CC BY-SA 4.0 |
| 缺 iOS 兼容测试条目 | 12.1 专项测试条目 |

### 17.3 许可（License）

- **代码**：MIT License（开源、可商用、注明版权）。
- **内容**（图文、公式讲解、术语表）：CC BY-SA 4.0（署名—相同方式共享），鼓励教学复用并回馈。
- 第三方资源（KaTeX、字体、D3 子模块）遵循其各自许可，集中列于 `/about` 与仓库 `LICENSES`。
